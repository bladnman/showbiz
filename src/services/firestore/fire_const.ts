// const level_prefix = "matt";
const level_prefix = "test";

export const FIRE_SHOWS_COLLECTION_NAME = `${level_prefix}_shows`; // matt_shows, shows
export const FIRE_CUSTOM_DATA_COLLECTION_NAME = `${level_prefix}_custom_data`;
export const FIRE_BOARD_COLLECTION_NAME = `${level_prefix}_boards`;
